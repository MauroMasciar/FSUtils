<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.masciar.fsutils" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CETAActivity"
            android:label="@string/title_activity_ceta" >
        </activity>
        <activity
            android:name=".RegALT"
            android:label="@string/title_activity_reg_alt" >
        </activity>
        <activity
            android:name=".CTASActivity"
            android:label="@string/title_activity_ctas" >
        </activity>
    </application>

</manifest>
